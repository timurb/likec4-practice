model {
    extend kafka_cluster {
        metricsExporter kafkaExporter {
            kafkaProcess -[metrics]-> monitoring.prometheus
            kafka -[metrics]-> this "Pulls metrics"
        }        
    }
}